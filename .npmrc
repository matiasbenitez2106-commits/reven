# @vercel/analytics declara un peer OPCIONAL de @sveltejs/kit (cruft viejo del
# árbol) que en un proyecto Next/React no aplica y dispara un falso ERESOLVE.
# legacy-peer-deps=true hace que npm (acá y en Vercel) no trabe por ese peer.
legacy-peer-deps=true
